# 2 - Leia um número fornecido pelo usuário. Se esse número for positivo, calcule a raiz quadrada no número.
# Se o número for negativo, mostre uma mensagem dizendo que o número é inválido

import math

n = int(input('Informe um número: '))

if n > 0:
    raiz = math.sqrt(n)
    print(f'A raiz quadrada do numero {n} é {raiz}')

else:
    print(f'Número invalido')
